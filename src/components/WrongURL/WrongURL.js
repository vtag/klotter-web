import React from 'react'
import { connect } from 'react-redux'

/**/
import styles from './WrongURL.scss'

const WrongURL = (props) => {
  return (
    <div className={styles.wrapper}>
      wrong url
    </div>
  )
}

export default WrongURL
